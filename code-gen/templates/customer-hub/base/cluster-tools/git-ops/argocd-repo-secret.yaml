apiVersion: v1
kind: Secret
metadata:
  name: argo-git-deploy
  namespace: argocd
  labels:
    argocd.argoproj.io/secret-type: repository
data:
  url: ${CLUSTER_STATE_REPO_URL_BASE64}
  type: Z2l0
  sshPrivateKey: |
     ${SSH_ID_KEY_BASE64}